from django.db import models

# This app has no models of its own — it provides the staff-facing
# dashboard and CRUD screens on top of the hotels, rooms, guests,
# reservations, billing and housekeeping models.
